import { Injectable, Logger } from '@nestjs/common';

@Injectable()
export class MapsService {
  private readonly logger = new Logger(MapsService.name);

  // TODO: Sprint 3 — Google Maps API integration
}
